export interface Project {
  url: string; // github repo
  html_url?: string; // github pages site
  name: string;
  created_at: string;
  updated_at: string;
  pushed_at: string;
  description: string;
}
